# 2026年8月11日 — 基础研究与硬件选型

## 1. Python 基础复习

- 复习 Python 基础知识。
- 学习如何阅读和理解项目中需要使用的 Python 代码，为后续开发做好准备。

## 2. OpenCV 环境配置

- 了解 OpenCV 的基本功能及其在计算机视觉项目中的作用。
- 完成 OpenCV 的安装与基本环境配置。

## 3. 摄像头测试与人员识别

- 使用电脑自带摄像头进行初步测试。
- 尝试使用 Python 和 OpenCV 获取实时摄像头画面。
- 对实时图像进行初步处理，并尝试实现真实人物的识别。

## 4. 硬件方案选型

经过初步调研与比较，确定目前计划使用以下硬件：

- **ESP32 38-Pin Wi-Fi + Bluetooth Development Board (DevKit V1)**：作为系统控制器，负责接收控制信号并控制外部设备。
- **5V 1-Channel Relay Module with Optocoupler Isolation**：用于实现控制信号与灯光设备之间的电气控制。

**阶段成果:**
完成了项目初期的 Python 与 OpenCV 基础准备，并完成了摄像头的初步测试。同时确定了后续智能照明系统的主要硬件方案。

# 日期：2026年8月18日

今天，我决定改变智能照明项目的开发方向。

在项目开始的时候，我原本计划使用摄像头和基于 AI 的人体检测模型来判断房间内是否有人。如果检测到有人，系统就自动开灯；如果没有检测到人，则自动关灯。

但是，在进一步思考这个项目的实际应用场景后，我意识到，我未来更希望开发的是一个基于自然交互方式的 **Smart Home（智能家居）系统**。

因此，我决定将原来的**摄像头人体检测**改为**语音识别**。

这次改变的主要原因是，我认为语音交互更加符合我未来想探索的智能家居方向。与其通过摄像头持续监控环境，不如让用户直接通过语音发出指令。例如，用户可以说“开灯”或“关灯”，系统识别语音指令后，再控制灯具执行相应的操作。

我也开始考虑**隐私问题**。摄像头需要持续采集周围的图像信息，这在家庭环境中可能会带来一定的隐私顾虑。虽然语音控制也需要采集声音，但对于目前这个项目来说，它能够在不持续进行视觉监控的情况下，实现更加自然的设备控制。

除此之外，我认为语音控制具有更好的**扩展性**。如果未来将这个项目扩展成一个更加完整的 Smart Home 系统，那么相同的语音控制架构还可以用于控制风扇、空调以及其他智能家居设备。

因此，我目前计划将系统设计为：

```text
麦克风
  ↓
语音识别
  ↓
指令识别
  ↓
ESP32
  ↓
继电器
  ↓
灯具开启 / 关闭
```

目前，我会先实现“开灯”和“关灯”这两个最基本的语音指令。在系统能够稳定运行之后，我计划继续增加更多语音指令，并逐步将这个项目扩展成一个更加完整的 Smart Home 系统。

这次项目方向的调整也让我认识到：

> **工程不仅仅是让一个方案能够运行，更重要的是思考这个方案是否真正适合实际应用，以及它是否能够支持未来的发展。**

接下来，我将开始制作第一个可以通过语音控制灯具的原型。










