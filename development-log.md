# 2026年8月11日 — 基础研究与硬件选型

## 1. Python 基础复习

- 复习 Python 基础知识。
- 学习如何阅读和理解项目中需要使用的 Python 代码，为后续开发做好准备。

## 2. OpenCV 环境配置

- 了解 OpenCV 的基本功能及其在计算机视觉项目中的作用。
- 完成 OpenCV 的安装与基本环境配置。

## 3. 摄像头测试与人员识别

- 使用电脑自带摄像头进行初步测试。
- 尝试使用 Python 和 OpenCV 获取实时摄像头画面。
- 对实时图像进行初步处理，并尝试实现真实人物的识别。

## 4. 硬件方案选型

经过初步调研与比较，确定目前计划使用以下硬件：

- **ESP32 38-Pin Wi-Fi + Bluetooth Development Board (DevKit V1)**：作为系统控制器，负责接收控制信号并控制外部设备。
- **5V 1-Channel Relay Module with Optocoupler Isolation**：用于实现控制信号与灯光设备之间的电气控制。

**阶段成果:**
完成了项目初期的 Python 与 OpenCV 基础准备，并完成了摄像头的初步测试。同时确定了后续智能照明系统的主要硬件方案。

# 项目日志：从人体检测到语音控制——一次系统设计方向的调整

**日期：2026年8月18日**

---

## 1. 项目方案的初始设计

在项目初期，我计划通过**摄像头**和 **AI 人体检测技术**判断房间内是否有人，并根据检测结果自动控制灯具的开启和关闭。

初始技术路线如下：

```text
摄像头采集图像
        ↓
AI 人体检测
        ↓
判断是否有人
        ↓
ESP32
        ↓
继电器
        ↓
灯具开启 / 关闭
```

---

## 2. 为什么改变项目方向？

在进一步思考项目的实际应用场景后，我决定重新设计系统的交互方式，将原来的**人体视觉检测**调整为**语音控制**。

这次改变并不是简单地更换一种识别技术，而是基于我对未来项目方向的重新思考。

我希望这个项目能够成为未来 **Smart Home（智能家居）系统**的一个基础模块。

如果继续使用摄像头作为主要交互方式，系统需要持续采集和处理用户周围的图像信息。虽然这种方式可以实现人体检测，但在家庭环境中可能存在一定的**隐私问题**，同时系统的控制方式也相对单一。

相比之下，**语音交互更加符合智能家居的使用场景**。

例如，用户可以直接说：

> “开灯”

或者：

> “关灯”

系统识别语音指令后即可执行相应操作。

这种交互方式更加直接，也更容易进一步扩展到其他智能家居功能，例如控制风扇、空调或其他家用设备。

---

## 3. 新的系统设计

因此，我将系统的技术路线调整为：

```text
麦克风采集语音
        ↓
语音识别
        ↓
关键词 / 指令判断
        ↓
ESP32
        ↓
继电器
        ↓
灯具开启 / 关闭
```

新的系统将主要通过识别用户的语音指令来控制灯具。

例如：

| 语音指令 | 系统操作 |
| :--: | :--: |
| `开灯` | 开启灯具 |
| `关灯` | 关闭灯具 |

---

## 4. 下一步计划

在第一阶段，我计划先实现 **“开灯”** 和 **“关灯”** 两个基本语音指令。

主要目标包括：

* [ ] 实现麦克风实时采集声音
* [ ] 实现语音识别
* [ ] 实现“开灯”关键词识别
* [ ] 实现“关灯”关键词识别
* [ ] 建立 PC / Laptop 与 ESP32 之间的通信
* [ ] 通过 ESP32 控制继电器
* [ ] 实现语音指令与灯具状态之间的联动
* [ ] 测试不同环境噪声下的识别准确率
* [ ] 测试系统整体响应时间和稳定性

在系统稳定运行后，我计划进一步增加更多语音指令，并尝试将该系统扩展为更加完整的 **Smart Home** 控制系统。

---

## 5. 本次调整带来的思考

这次方案调整让我认识到，一个好的工程项目不仅需要考虑：

> **“技术上能不能实现？”**

还需要考虑：

* 实际使用场景
* 用户体验
* 隐私
* 系统扩展性
* 未来的发展方向

因此，我认为这次从**“视觉人体检测”转向“语音控制”**的调整，不仅使当前的智能照明项目更加符合 Smart Home 的发展方向，也让我开始从更加完整的系统设计角度思考项目。

同时，这也为后续构建更加完整的智能家居系统打下了基础。

**下一步计划:**
研究如何提高摄像头的人体识别准确率，并实现计算机与 ESP32 之间的通信连接。
